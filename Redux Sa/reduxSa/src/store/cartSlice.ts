import { createSlice } from "@reduxjs/toolkit";

type cartItem = {}

type cartState = {
  items : cartItem[]
}

const initialState =


export const cartSlice = createSlice({
  name: "cart",
  initialState : 
});
